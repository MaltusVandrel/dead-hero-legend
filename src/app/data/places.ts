import { Place } from "../model/system/place";


let places:Place[]=[];
places.push(new Place(["This is the first text.","Be happy that it works for now!"],'DEMOS_SIMPLE_TEXT'));

export let placesList:Place[]=places;